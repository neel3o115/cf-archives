#include<iostream>
#include<string>
using namespace std;

int main(){
    int t;cin>>t;
    while(t--){
        string a,b;cin>>a>>b;
        int i=0,j=0;
        int count1=0,maxm=0;
        while(i<a.size() and j<b.size()){
            if(a[i]!=b[j]){
                j++;
            }else{
                count1++;
                for(int k=1;k<a.size();k++){
                    if(a[k])
                }
            }
        }
    }
}